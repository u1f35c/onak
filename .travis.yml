language: c
dist: bionic
compiler:
  - clang
  - gcc
addons:
  apt:
    packages:
      - cmake
      - libdb-dev
      - libcurl4-gnutls-dev
      - libsystemd-dev
      - nettle-dev
before_script:
  - mkdir travis-build
  - cd travis-build
  - cmake ..
script: make && make test
